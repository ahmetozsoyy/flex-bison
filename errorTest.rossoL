-- Açıklama satırı
x <- 3;
print(x);
x <- 3;
while (x > 2) {
    print(x);
    x <- x - 1;
}
print(3 + 4);
x <- 5;
print(x * 2);
x <- 100;
y <- 100;

while (1) {
    if (key_pressed(LEFT)) {
        x <- x - 5;
    }
    if (key_pressed(RIGHT)) {
        x <- x + 5;
    }
    if (key_pressed(UP)) {
        y <- y - 5;
    }
    if (key_pressed(DOWN)) {
        y <- y + 5;
    }

    draw_circle(x, y, 20);
}